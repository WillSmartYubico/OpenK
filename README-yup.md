# yup

This change will effectively disable the user presence check, instead simply waiting 100ms.

If this defect is present, the authenticator will include "yup" in the list of options returned during the getinfo command.

Code changes are labeled defect-yup
